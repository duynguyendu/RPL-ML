\section{Data Collection for Model Training}
The data is collected when the a node switch parent and when a node receives its parent DIO and decides not to switch parent.
Afterward, the PDR is calculated by chunking the TX and RX withing the current row and the next row of the same node.

In the end, we have collected 11 routing metrics:
ETX:
Hop count:
RSSI:

PPM:
CPU util:
Drop rate:

Parent PPM
Parent CPU util:
Parent neighbour counts:
